
@model CardLookupViewModel
@{
    ViewData["Title"] = "Inicio";
}

<div class="page-header">
    <h1>Consultar estado de cuenta</h1>
    <span class="subtitle">Ingresa el número del documento del cliente para continuar</span>
</div>

<div class="card" style="max-width: 480px;">

    <form asp-controller="Home" asp-action="Index" method="post" novalidate>
        <div asp-validation-summary="ModelOnly" class="validation-summary"></div>

        <div class="form-group">
            <label asp-for="docNumber"></label>
            <input asp-for="docNumber" class="form-control" placeholder="123456789" />
            <span asp-validation-for="docNumber" class="field-error"></span>
            <div class="field-hint">Ingrese un DUI o NIT.</div>
        </div>

        <div class="btn-row">
            <button type="submit" class="btn btn-primary">Ver estado de cuenta</button>
        </div>
    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
